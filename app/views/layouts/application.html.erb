<!DOCTYPE html>
<html>
<head>
  <title>Geetao</title>
  <meta charset="utf-8" /> 
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=1000, initial-scale=1.0, maximum-scale=1.0">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
    <!-- HTML5 shim, for IE6-8 support of HTML5 elements. All other JS at the end of file. -->
    <!--[if lt IE 9]>
<%= javascript_include_tag "html5shiv" %>
<%= javascript_include_tag "respond.min" %>
    <![endif]-->
</head>
<body>
  <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
        </button>
        <%= link_to('GeeTao', root_path, class: "navbar-brand")  %>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">行程<span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to('浏览行程', trips_path)  %></li>
              <li><%= link_to('创建行程', new_trip_path)  %></li>
            </ul>
          </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li>
            <% if user_signed_in? %>
              <%= link_to(current_user.name, edit_user_registration_path) %>
            <% else %>
              <%= link_to('注册新用户', new_user_registration_path) %>
            <% end %>
          </li>
          <li>
            <% if user_signed_in? %>
            <%= link_to('退出', destroy_user_session_path, :method => :delete) %>
            <% else %>
            <%= link_to('登录', new_user_session_path)  %>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </div>
  
  <div class="container content">

    <!--notice -->
    <% if notice %>
      <div class="alert alert-info alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <%= notice  %>
      </div>
    <% end %>
    <!--.notice -->

    <!--alert -->
    <% if alert %>
      <div class="alert alert-info alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <%= alert %>
      </div>
    <% end %>
    <!--.alert -->
    <%= yield :carousel %>
    <%= yield %>

  </div>
</body>
</html>
